
#region grassRender
/*
                float distance = DistanceFromObject(Comps[grassAlphaNum].Transform.Translation, Camera.Position);

                if (distance > 100)
                    Comps[grassAlphaNum].tex = grassUltraLowDef;
                else  if (distance > 40)
                    Comps[grassAlphaNum].tex = grassLowDef;
                else if (distance > 20)
                    Comps[grassAlphaNum].tex = grassMidDef;
                else
                    Comps[grassAlphaNum].tex = grassMidDef;

                /*Render Grass*/
/* Comps[grassAlphaNum].Transform.Translation += new Vector3(0, 0,0);

 if (tech.Equals("DepthMapShader")) Comps[grassAlphaNum].Draw(gameTime, "DepthMapShader");
 else Comps[grassAlphaNum].Draw(gameTime, "Alpha");                
 /*Comps[grassAlphaNum].Transform.Translation += new Vector3(0, 0, 7);
 Comps[grassAlphaNum].Draw(gameTime, "Alpha");
 Comps[grassAlphaNum].Transform.Translation += new Vector3(6, 0, 0);
 Comps[grassAlphaNum].Draw(gameTime, "Alpha");
 Comps[grassAlphaNum].Transform.Translation += new Vector3(0, 0, -7);
 Comps[grassAlphaNum].Draw(gameTime, "Alpha");
 Comps[grassAlphaNum].Transform.Translation += new Vector3(-6, 0, -7);
 Comps[grassAlphaNum].Draw(gameTime, "Alpha");
 Comps[grassAlphaNum].Transform.Translation -= new Vector3(0, 0, 0);*/
#endregion